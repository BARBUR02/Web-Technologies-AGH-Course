export interface Car {
        brand: string;
        model: string;
        colors: string[];
        features: {
        engine: number;
        type: string;
        length: string;
        width: string; 
    }
}